import { PriceByActivityPipe } from './price-by-activity.pipe';

describe('PriceByActivityPipe', () => {
  it('create an instance', () => {
    const pipe = new PriceByActivityPipe();
    expect(pipe).toBeTruthy();
  });
});
